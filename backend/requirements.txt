# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Data + physics (real solar nowcasting)
pvlib>=0.11.0
pandas>=2.2.0
numpy>=2.1.0
# Columnar ML dataset storage (Phase 1.7 parquet files)
pyarrow>=16.0.0
# Excel reader for Kaggle load workbook (hourly-load-india .xlsx)
openpyxl>=3.1.0

# Machine learning (Phase 1.5 solar forecasting)
scikit-learn>=1.5.0
joblib>=1.4.0
# Kaggle dataset ingestion (optional; manual CSV placement works without it)
kaggle>=1.6.0

# Reinforcement Learning (digital twin)
gymnasium>=1.0.0

# Scheduling (opt-in real-time ingestion)
apscheduler>=3.10.0

# HTTP client (weather provider + tests)
httpx>=0.27.0

# Persistence + cache (SQLite default for local; PostgreSQL/TimescaleDB for prod)
sqlalchemy[asyncio]>=2.0.35
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.1.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
